{
    "name": "i-am-tom/schemer",
    "description": "A Joi-like interface for validating data structures.",
    "license": "ISC",
    "version": "2.1.0",
    "require-dev": {
        "kahlan/kahlan": "^3.0"
    },
    "scripts": {
        "test": "vendor/bin/kahlan --spec=test --pattern=*.php --reporter=verbose"
    },
    "type": "library",
    "autoload": {
        "psr-4": {
            "Schemer\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Schemer\\Test\\": "test/"
        }
    }
}
